const widgets = [];

export const getAll = () => widget;

export const add = newWidget => widgets.push(newWidget);

export default {
    getAll : () => widget,
    dd : newWidget => widgets.push(newWidget)
}